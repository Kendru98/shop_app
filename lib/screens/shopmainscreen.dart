import 'package:flutter/material.dart';

class ShopHomePage extends StatelessWidget {
  const ShopHomePage({Key? key, required String title}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
